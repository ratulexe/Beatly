name: Deployment Placeholder

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout Code
      uses: actions/checkout@v4

    - name: Confirm Manual Deployment Required
      run: |
        echo "No production deployment is configured yet."
        echo "Choose backend/frontend platforms and add provider-specific deployment steps before enabling automatic deploys."
